import React from 'react';

const GridButton = ({ onClick }) => {
  return (
    <button onClick={onClick} className="grid-button">
      {/* Add grid button icon or text as needed */}
    </button>
  );
};

export default GridButton;
